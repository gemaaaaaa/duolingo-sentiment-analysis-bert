[project]
name = "bertbird"
version = "0.1.0"
description = "Interactive web demo to analyze Duolingo Play Store reviews sentiment using fine-tuned BERT"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "hf-xet>=1.2.0",
    "safetensors>=0.7.0",
    "streamlit>=1.52.2",
    "torch>=2.9.1",
    "transformers[torch]>=4.57.3",
]
